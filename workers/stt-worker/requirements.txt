# ─────────────────────────────────────────────────────────
# Core (mock 엔진만 사용 시에도 필수)
# ─────────────────────────────────────────────────────────
supabase==2.9.1
python-dotenv==1.0.1


# ─────────────────────────────────────────────────────────
# OpenAI Whisper API 엔진 (STT_ENGINE=openai)
# 25MB 단일 요청 한도가 있으므로 STT_CHUNK_SEC=600 권장.
# ─────────────────────────────────────────────────────────
# openai>=1.0


# ─────────────────────────────────────────────────────────
# 로컬 STT (STT_ENGINE=local_whisper)
# faster-whisper 가 OpenAI Whisper 대비 2~4배 빠르고 메모리 효율적.
# 시스템에 ffmpeg 설치가 필요합니다.
#
#   Ubuntu/Debian:  sudo apt install ffmpeg
#   macOS:          brew install ffmpeg
#   Windows:        choco install ffmpeg  (또는 https://ffmpeg.org)
#
# GPU 사용 시 추가로:
#   pip install ctranslate2[cuda]  (CUDA 12 환경)
# ─────────────────────────────────────────────────────────
# faster-whisper>=1.0


# ─────────────────────────────────────────────────────────
# 청크 분할에는 ffmpeg/ffprobe 가 필요합니다 (위 안내 참조).
# 미설치 시 분할 없이 단일 호출로 fallback 됩니다.
# ─────────────────────────────────────────────────────────


# ─────────────────────────────────────────────────────────
# 향후 (Claude 요약 활성화 시)
# ─────────────────────────────────────────────────────────
# anthropic>=0.39
